<h2><%= t(:show, :default => "Show") %> <%= t(:page, :default => "page") %></h2>

  <p>
    <b><%= t(:title, :default => "Title") %>:</b>
      <%= @page.title %>
  </p>
  <p>
    <b><%= t(:content, :default => "Content") %>:</b>
      <%= @page.content %>
  </p>

<!-- Beautiful_scaffold - AddField - Field - Do not remove -->

<%= link_to t(:edit, :default => "Edit"), edit_admin_page_path(@page), :class => "btn" %>
<%= link_to t(:back, :default => "Back"), admin_pages_path, :class => "btn" %>

